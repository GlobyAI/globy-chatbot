import React from 'react'

type Props = {}

export default function Form({ }: Props) {
    return (
        <form className="textfield-container">
            <input type="text" />
        </form>
    )
}