---
phase: 03-foretagarna-visual-theme
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/style/base/components/_chatbox.scss
  - app/style/base/components/_sidebar.scss
  - app/style/base/components/_qualityScore.scss
autonomous: true
requirements: [THEME-04, THEME-05, THEME-07, THEME-08, THEME-09]

must_haves:
  truths:
    - "Chat input area uses themed surface and accent colors"
    - "Sidebar uses themed panel background, text colors, and accent highlights"
    - "Quality score card uses themed gradient or surface colors"
    - "Shadows on chatbox and sidebar elements use shadow tokens"
  artifacts:
    - path: "app/style/base/components/_chatbox.scss"
      provides: "Themed chat input area"
      contains: "var(--color-surface-input)"
    - path: "app/style/base/components/_sidebar.scss"
      provides: "Themed sidebar"
      contains: "var(--color-surface-secondary)"
    - path: "app/style/base/components/_qualityScore.scss"
      provides: "Themed quality score card"
      contains: "var(--color-accent)"
  key_links:
    - from: "app/style/base/components/_chatbox.scss"
      to: "app/style/abstracts/variables/_theme-tokens.scss"
      via: "CSS custom property consumption"
      pattern: "var\\(--color-surface-input\\)"
    - from: "app/style/base/components/_sidebar.scss"
      to: "app/style/abstracts/variables/_theme-tokens.scss"
      via: "CSS custom property consumption"
      pattern: "var\\(--color-surface-secondary\\)"
---

<objective>
Convert the chatbox (message input area), sidebar, and quality score components from hardcoded SCSS color variables to CSS custom properties, enabling them to render correctly in both Globy dark theme and Foretagarna light theme.

Purpose: These three components form the primary interactive surfaces — the input area where users type, the sidebar navigation/settings panel, and the quality score widget. All must respond to theme switching.
Output: Themed _chatbox.scss, _sidebar.scss, _qualityScore.scss
</objective>

<execution_context>
@/home/berre/.claude/get-shit-done/workflows/execute-plan.md
@/home/berre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-foretagarna-visual-theme/03-01-SUMMARY.md

<interfaces>
<!-- Token names available from Plan 01 (expanded _theme-tokens.scss) -->
Surface: --color-surface-primary, --color-surface-secondary, --color-surface-elevated, --color-surface-input, --color-surface-overlay
Messages: --color-bubble-user, --color-bubble-user-text, --color-bubble-bot-text
Text: --color-text, --color-text-light, --color-text-heading, --color-text-inverse, --color-text-muted
Interactive: --color-interactive-bg, --color-interactive-hover
Shadows: --shadow-card, --shadow-hover, --shadow-popup
Accent: --color-accent, --color-accent-hover, --color-accent-subtle, --color-accent-border
Shape: --radius, --radius-lg
Typography: --font-family, --fs-body, --fs-small, --font-weight-heading, --lh-body
Transition: --transition-default

<!-- SCSS variables that map to tokens (for reference during conversion) -->
$black-500 (#262626) -> --color-surface-elevated
$black-700 (#1a1d21) -> --color-surface-primary
$black-900 (#02080f) -> --color-surface-secondary
$dark-gray (#333536) -> --color-surface-input
$gray-100 (#e5e5e5) -> --color-border (close enough) or keep for non-themed structural elements
$gray-300 (#b2b2b2) -> --color-text-muted
$gray-500 (#737373) -> --color-text-light
$gray-800 (#4d4d4d) -> --color-interactive-hover
$gray-900 (#3d3e3f) -> --color-interactive-bg
$white-2 (#f2f2f2) -> --color-text-heading or --color-text (depends on context)
$purple (#b239e8) -> --color-accent
$dark-purple (#6b228b) -> --color-bubble-user (or --color-accent-hover for non-bubble uses)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert chatbox input area and sidebar to CSS custom properties</name>
  <files>app/style/base/components/_chatbox.scss, app/style/base/components/_sidebar.scss</files>
  <read_first>
    - app/style/base/components/_chatbox.scss
    - app/style/base/components/_sidebar.scss
    - app/style/abstracts/variables/_theme-tokens.scss
    - app/style/abstracts/variables/_colors.scss
  </read_first>
  <action>
**In `_chatbox.scss`:**

Replace hardcoded SCSS color variables with CSS custom properties. Specific replacements:

1. `.chat-box` — `background-color: $dark-gray` becomes `background-color: var(--color-surface-input)`, `box-shadow: 0px 6px 20px 0px rgba($color: $black-950, $alpha: 0.4)` becomes `box-shadow: var(--shadow-card)`
2. `.has-image .icons--plus` and `.has-text .icons--plus` and `.has-value .icons--plus` — `background-color: $gray-900` becomes `background-color: var(--color-interactive-bg)`
3. `.has-image .icons--send` and `.has-text .icons--send` and `.has-value .icons--send` — `background-color: $purple` becomes `background-color: var(--color-accent)`
4. `.has-value .chat-box__actions` — `background-color: $dark-gray` becomes `background-color: var(--color-surface-input)`
5. `.textfield textarea` — `@include text($white-2, 400, $text-md)` becomes `@include text(var(--color-text), 400, $text-md)`, placeholder `$gray-300` becomes `var(--color-text-muted)`
6. `.textfield .images-container .selected-img` — `border: 1px solid $gray-500` becomes `border: 1px solid var(--color-border)`
7. `.textfield .images-container .delete-img` — `background-color: $gray-500` becomes `background-color: var(--color-interactive-bg)`, hover `$gray-800` becomes `var(--color-interactive-hover)`
8. `.textfield .images-container figcaption` — `@include text($white-2, ...)` becomes `@include text(var(--color-text), ...)`
9. `&__actions .icons` — keep structural styles, only change colors
10. `.icons--plus` base (non-state) — `background-color: $gray-900` becomes `background-color: var(--color-interactive-bg)`, and the xl breakpoint `background-color: transparent` stays as-is
11. `.options-popup` — `background-color: $gray-900` becomes `background-color: var(--color-surface-elevated)`, `box-shadow: 0px 3px 10px 1px rgba($color: $black-700, $alpha: 0.4)` becomes `box-shadow: var(--shadow-popup)`, `@include text($white-2, ...)` becomes `@include text(var(--color-text), ...)`
12. `.textfield ::-webkit-scrollbar-thumb` — `background: $gray-500` becomes `background: var(--color-text-light)`

**In `_sidebar.scss`:**

Replace hardcoded SCSS color variables with CSS custom properties:

1. `.sidebar-overlay` — `background-color: rgba($color: $black-950, $alpha: 0.25)` becomes `background-color: var(--color-surface-overlay)`
2. `.sidebar` — `background-color: $black-900` becomes `background-color: var(--color-surface-secondary)`, `border-top-right-radius` and `border-bottom-right-radius` stay as-is
3. `.sidebar__toggle-icon` — `background-color: $black-900` becomes `background-color: var(--color-surface-secondary)`, `border: 1px solid $gray-800` becomes `border: 1px solid var(--color-border)`
4. `.sidebar__close` — `background-color: $black-900` becomes `background-color: var(--color-surface-secondary)`
5. `.style-toggle` throughout — `@include text($white-2, 500, $text-md)` becomes `@include text(var(--color-text-heading), 500, $text-md)`
6. `.style-options > b` labels — `@include text($gray-100, 400, $text-sm)` becomes `@include text(var(--color-text), 400, $text-sm)`
7. `.style-options > p` descriptions — `@include text($gray-300, 400, $text-sm)` becomes `@include text(var(--color-text-muted), 400, $text-sm)`
8. `.uploaded-img .close` — `border: 1px solid $gray-500` becomes `border: 1px solid var(--color-border)`, `background-color: $black-900` becomes `background-color: var(--color-surface-secondary)`, `color: $white-2` becomes `color: var(--color-text)`
9. `.profile .user-info > strong` — `@include text($white-2, ...)` becomes `@include text(var(--color-text-heading), ...)`
10. `.profile .user-info > p` — `@include text($gray-100, ...)` becomes `@include text(var(--color-text-muted), ...)`
11. `.settings` popup — `background-color: $black-500` becomes `background-color: var(--color-surface-elevated)`, `box-shadow` with `$black-900` becomes `box-shadow: var(--shadow-popup)`, `@include text($white-2, ...)` becomes `@include text(var(--color-text), ...)`
12. `.setup-booking-btn` — `rgba($purple, 0.15)` gradients become `var(--color-accent-subtle)` for background, `border: 0.1rem solid rgba($purple, 0.3)` becomes `border: 0.1rem solid var(--color-accent-border)`. The hover states and gradient colors referencing `$purple` become `var(--color-accent)`. Keep the animation keyframes as-is.
13. `.booking-btn__icon` — `background: linear-gradient(135deg, $purple 0%, #6366f1 100%)` becomes `background: var(--color-accent)` (simplified for theming)
14. `.booking-btn__text` — `@include text($white-2, ...)` becomes `@include text(var(--color-text-heading), ...)`
15. `.booking-btn__arrow` — `color: $gray-300` becomes `color: var(--color-text-muted)`
16. `.booking-info` — `@include text($gray-500, ...)` becomes `@include text(var(--color-text-light), ...)`
17. `.profile__instagram-btn` hover — `rgba(#e1306c, 0.4)` and `rgba(#e1306c, 0.1)` can stay as-is (Instagram branding, not themed)

IMPORTANT: Keep the `@use "../../abstracts" as *` import. Keep all structural properties (display, flex, position, padding, width, height, z-index, overflow, transform, breakpoints, transition timings). Only change COLOR-related properties. Font SIZE variables ($text-sm, $text-md, etc.) stay as SCSS variables.
  </action>
  <verify>
    <automated>cd /home/berre/gits/globy-chatbot && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - _chatbox.scss contains `background-color: var(--color-surface-input)` for .chat-box
    - _chatbox.scss contains `box-shadow: var(--shadow-card)` for .chat-box
    - _chatbox.scss contains `background-color: var(--color-accent)` for .icons--send active state
    - _chatbox.scss contains `var(--color-text)` for textarea text color
    - _chatbox.scss contains `var(--color-surface-elevated)` for .options-popup
    - _sidebar.scss contains `background-color: var(--color-surface-secondary)` for .sidebar
    - _sidebar.scss contains `var(--color-surface-overlay)` for .sidebar-overlay
    - _sidebar.scss contains `var(--color-text-heading)` for .style-toggle and user-info strong
    - _sidebar.scss contains `var(--color-surface-elevated)` for .settings popup
    - _sidebar.scss contains `box-shadow: var(--shadow-popup)` for .settings
    - Both files still contain `@use "../../abstracts" as *`
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Chatbox and sidebar components use CSS custom properties for all themed colors. Structural properties unchanged. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Convert quality score card to CSS custom properties</name>
  <files>app/style/base/components/_qualityScore.scss</files>
  <read_first>
    - app/style/base/components/_qualityScore.scss
    - app/style/abstracts/variables/_theme-tokens.scss
  </read_first>
  <action>
Replace hardcoded SCSS color variables with CSS custom properties in `_qualityScore.scss`:

1. `.quality-score` — `background: linear-gradient(135deg, $dark-purple, $black-900)` becomes `background: linear-gradient(135deg, var(--color-accent-hover), var(--color-surface-secondary))`, `color: $white-2` becomes `color: var(--color-text-heading)`, `box-shadow: 0 0.8rem 2rem rgba(0, 0, 0, 0.25)` becomes `box-shadow: var(--shadow-hover)`
2. `&__title` — `@include text($gray-100, 400, $text-sm)` becomes `@include text(var(--color-text-muted), 400, $text-sm)`
3. `&__icon` — `color: $gray-300` becomes `color: var(--color-text-muted)`
4. `.brand-insight-tip` — `background-color: $black-700` becomes `background-color: var(--color-surface-primary)`, `@include text($white-2, ...)` becomes `@include text(var(--color-text), ...)`
5. `&__percentage` — `@include text($white-2, 400, 4.8rem)` becomes `@include text(var(--color-text-heading), 400, 4.8rem)`
6. `&__footer-title` — `@include text($white-2, 500, $text-lg)` becomes `@include text(var(--color-text-heading), 500, $text-lg)`
7. `&__footer-text` — `@include text($white-2, 400, $text-xs)` becomes `@include text(var(--color-text-muted), 400, $text-xs)`

Keep `@use "../../abstracts" as *` import and all structural properties.
  </action>
  <verify>
    <automated>cd /home/berre/gits/globy-chatbot && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - _qualityScore.scss contains `var(--color-accent-hover)` in the gradient background
    - _qualityScore.scss contains `var(--color-surface-secondary)` in the gradient background
    - _qualityScore.scss contains `var(--color-text-heading)` for percentage and footer title
    - _qualityScore.scss contains `var(--color-text-muted)` for title and footer text
    - _qualityScore.scss still contains `@use "../../abstracts" as *`
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Quality score card uses CSS custom properties. The gradient adapts to theme (accent-hover to surface-secondary). Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `grep -r "var(--color-surface-input)" app/style/base/components/_chatbox.scss` returns matches
- `grep -r "var(--color-surface-secondary)" app/style/base/components/_sidebar.scss` returns matches
- `grep -r "var(--color-accent-hover)" app/style/base/components/_qualityScore.scss` returns matches
- No remaining `$purple` references in _chatbox.scss or _sidebar.scss (except possibly in `@keyframes` or non-themed decorative uses)
</verification>

<success_criteria>
- All three component files consume CSS custom properties for colors
- Chatbox input area, sidebar panel, and quality score card all respond to data-theme attribute
- Build succeeds
- No hardcoded Globy-specific color variables remain in themed properties
</success_criteria>

<output>
After completion, create `.planning/phases/03-foretagarna-visual-theme/03-02-SUMMARY.md`
</output>
