

export default function Continue() {
    return (
        <div className="continue">
            <button className="btn btn--primary">Continue refining</button>
            <button className="btn btn--secondary">Move on for now</button>
        </div>
    )
}